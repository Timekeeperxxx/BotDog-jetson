# 前端交付修复验证（2026-07-18）

## 已验证修复

- 建图前执行无副作用的雷达快速预检；topic 缺失、无发布者、无实时数据和超时均阻止启动并显示明确告警。前端和后端建图入口都有防线，直接请求后端也不能绕过。
- 390px 导航页不再由账户栏或场景信息遮挡工具条；“点云图层”可正常点击，弹层未超出视口。
- 场景信息区分 `ground` 文件点数和最终场景点数。
- 全局渲染错误边界、确认框语义、Escape 关闭、Tab 焦点约束和关闭后焦点恢复已补齐。
- 后台磁盘 80%/90% 告警色、危险操作可访问名称和关键低对比度文本已修复。
- 旋转中心标记仅在相机交互或阻尼运动期间显示，停止后隐藏。
- 最终点云保持按区域密度降采样、全部最终瓦片追加加载且不随相机替换或回收。

## 自动验证结果

- 前端生产构建：通过。
- ESLint：通过。
- Vitest：17 个测试文件、54 项测试全部通过。
- Pytest：346 项测试全部通过。
- axe（登录、主操作台、导航桌面、后台、导航 390px）：5 个页面均为 0 项违规。
- 移动端点云图层命中测试：按钮中心命中按钮内容；弹层边界 `left=12, top=513, right=378, bottom=832`，无裁切。
- 浏览器页面异常：0。
- 当前无雷达实测：1.397 秒返回“雷达未连接：未发现原始数据 /livox/lidar”，且不会启动临时驱动。
- 当前运行页面端到端实测：确认建图后 2.002 秒显示同一明确告警，实际建图启动请求数为 0。
- Scene35 当前源点数 30,880,851，按 5cm 区域密度保留 30,343,139 点；最终 high 瓦片合计仍为 30,343,139 点，单瓦片阈值只拆文件、不丢点。
- `botdog-backend.service` 已重载新代码并恢复 active；其 systemd 依赖链监督重启了视频服务，`botdog-pipeline.service` 也已恢复 active。

## 验证边界

- 无头 Chromium 没有启用 WebGL，因此截图验证覆盖布局、层级和交互，不替代 Jetson 实机 GPU 点云渲染复验。
- 视频 WHEP 仍返回当前环境的 404；按本轮范围未修改视频链路。
