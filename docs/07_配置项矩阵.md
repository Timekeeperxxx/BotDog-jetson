# 配置项矩阵与参数约定

> **最后更新**：2026-07-02

配置来源分三层：

1. `.env` / 环境变量：由 `backend/config.py` 的 `Settings` 读取，是启动时配置的权威来源。
2. 数据库运行时配置：由 `backend/services_config.py` 的 `DEFAULT_CONFIGS` 初始化到 `system_config`，通过 `/api/v1/config` 管理。
3. 前端构建时配置：`VITE_API_BASE_URL`、`VITE_WHEP_URL` 等。

本文只列维护和联调最常用的配置。完整默认值以 `backend/config.py` 和 `backend/services_config.py` 为准。

## 启动级配置（`.env`）

| Key | 默认值 | 说明 |
|-----|--------|------|
| `BACKEND_HOST` | `0.0.0.0` | 后端监听地址 |
| `BACKEND_PORT` | `8000` | 后端监听端口 |
| `DATABASE_URL` | `sqlite+aiosqlite:///./data/botdog.db` | SQLite 数据库路径 |
| `AUTH_ENABLED` | `true` | 是否启用登录鉴权 |
| `AUTH_ADMIN_USERNAME` | `admin` | 初始化管理员用户名 |
| `AUTH_ADMIN_PASSWORD` | `admin123` | 初始化管理员密码，生产必须修改 |
| `JWT_SECRET` | `please_change_me_to_a_random_string` | JWT 签名密钥，生产必须修改 |
| `MAVLINK_SOURCE` | `simulation` | `simulation` 或 `mavlink` |
| `SIMULATION_WORKER_ENABLED` | `true` | 是否启用模拟遥测 |
| `CONTROL_ADAPTER_TYPE` | `unitree_b2` | `unitree_b2` 或 `simulation` |
| `UNITREE_NETWORK_IFACE` | `eno1` | Unitree B2 连接网卡 |
| `SAFETY_BLOCK_MOTION_WHEN_DISCONNECTED` | `true` | 底层断链时是否阻止运动 |

## AI 与自动跟踪

| Key | 默认值 | 说明 |
|-----|--------|------|
| `AI_ENABLED` | `true` | 是否启用 AI Worker |
| `AI_RTSP_URL` | `rtsp://127.0.0.1:8554/cam` | AI 拉流地址 |
| `AI_MODEL_PATH` | `models/helmet.engine` | 推理模型路径 |
| `AI_TARGET_CLASSES` | `["person", "head", "helmet"]` | 检测类别 |
| `AI_CONFIDENCE_THRESHOLD` | `0.4` | 置信度阈值 |
| `AI_FRAME_PROCESS_TIMEOUT_SECONDS` | `15.0` | 单帧推理超时 |
| `AUTO_TRACK_ENABLED` | `false` | 启动时是否默认启用自动跟踪 |
| `AUTO_TRACK_VX` | `0.4` | 自动跟踪线速度 |
| `AUTO_TRACK_VYAW` | `0.22` | 自动跟踪角速度 |
| `AUTO_TRACK_YAW_DEADBAND_PX` | `100` | 水平转向死区 |

## 导航与 ROS2

| Key | 默认值 | 说明 |
|-----|--------|------|
| `PCD_MAP_ROOT` | `/home/jetson/superlio/Super-LIO/src/super_lio/map` | 原始 PCD 地图目录 |
| `SCENE_MAP_ROOT` | `/home/jetson/Project/BOTDOG/MAPS` | 场景地图目录 |
| `NAV_WAYPOINT_STORE_DIR` | `./data/nav_waypoints` | 导航点存储目录 |
| `NAV_TASK_STORE_DIR` | `./data/nav_tasks` | 导航任务存储目录 |
| `ROS_NAV_ENABLED` | `true` | 是否启用 ROS2 导航桥 |
| `ROS_NAV_POSE_TOPIC` | `/amcl_pose` | 位姿来源 topic 或 TF 设置 |
| `ROS_NAV_START_TOPIC` | `/nav_start` | 导航任务启动 topic |
| `ROS_NAV_STOP_TOPIC` | `/nav_stop` | 导航停止 topic |
| `ROS_NAV_INITIAL_POSE_TOPIC` | `/initialpose` | 初始位姿 topic |
| `NAV_AUTO_TRACK_DURING_NAV_ENABLED` | `false` | 导航任务中是否允许自动跟踪联动 |

## 驱离与黄色区域识别

| Key | 默认值 | 说明 |
|-----|--------|------|
| `GUARD_MISSION_ENABLED` | `false` | 启动时是否启用驱离模式 |
| `GUARD_CONFIRM_TIME_S` | `1.5` | 入侵确认时间 |
| `GUARD_CLEAR_TIME_S` | `3.0` | 清空确认时间 |
| `GUARD_MAX_DURATION_S` | `120.0` | 单次驱离最大持续时间 |
| `GUARD_ALERT_AUDIO_PATH` | `assets/13282.wav` | 警告音频文件 |
| `ZONE_YELLOW_H_LOW/HIGH` | `14/42` | 黄色 HSV 色相范围 |
| `ZONE_MIN_AREA_PX` | `600` | 黄色区域最小面积 |
| `ZONE_ROI_TOP_RATIO` | `0.55` | 画面顶部跳过比例 |

## 运行时配置 API

| 接口 | 说明 |
|------|------|
| `GET /api/v1/config` | 查询 `system_config` 中的配置 |
| `POST /api/v1/config` | 更新配置，写入 `config_change_history` |
| `GET /api/v1/config/history` | 查询配置变更历史 |

`services_config.py` 当前包含以下配置类别：

| category | 说明 |
|----------|------|
| `backend` | 温度、心跳、WebSocket、视频看门狗、安全开关 |
| `hardware` | Unitree 网卡、MAVLink 端点 |
| `frontend_draw` | 前端画面区域绘制样式 |
| `zone` | 黄色区域检测阈值 |
| `storage` | 快照、遥测、录像保留策略 |
| `auto_track` | 自动跟踪参数 |
| `guard` | 驱离模式参数 |
| `nav` | 导航自动跟踪联动参数 |

## 前端构建配置

| Key | 说明 |
|-----|------|
| `VITE_API_BASE_URL` | 前端开发模式代理目标，默认 `http://127.0.0.1:8000` |
| `VITE_WHEP_URL` | 主视频 WHEP 地址；未设置时按页面 hostname 推导 |

## 修改原则

- 启动级硬件、路径、鉴权密钥优先放 `.env`。
- 需要后台页面运行时调整的参数放 `system_config`。
- 真机运动相关安全配置不要为方便调试长期关闭。
- 新增配置时同步更新 `backend/config.py`、`services_config.py`、管理后台展示和本文档。
